@model IEnumerable<NeoGym.Models.menu>

<ul class="navbar-nav  ">
    @foreach (var i in Model)
    {
        <li class="nav-item active ">
            <a class="nav-link" href="@Url.Action("Index",i.meta)"> @i.name </a>
        </li>
    }
</ul>
